# typed: strong

module Increase
  Unknown = Increase::Type::Unknown

  BooleanModel = Increase::Type::BooleanModel

  Enum = Increase::Type::Enum

  Union = Increase::Type::Union

  ArrayOf = Increase::Type::ArrayOf

  HashOf = Increase::Type::HashOf

  BaseModel = Increase::Type::BaseModel

  RequestParameters = Increase::Type::RequestParameters

  # This module contains various type declarations.
  module Type
  end
end
